services:
  mcp-imoveis:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp_imoveis
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${MCP_SERVER_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data:ro
